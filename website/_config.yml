# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Mekatronik 1
author: Samuel Åkesson
email: samak519@student.liu.se
description: >- # this means to ignore newlines until "baseurl:"
  Sida för kursen Mekatronik 1 på MTU mediagymnasiet.

# Build settings
markdown: kramdown
plugins:

# Include the CNAME file
include:
  - CNAME

# Change output folder from default _site to docs
destination: ../docs

# Enable livereload and watch by default
livereload: true

port: 3000
